project site

cosmo-->create-react-app f_cosmo
cd f_cosmo
npm install gh-pages --save-dev
npm remote add origin ...
----->package.json
         "homepage": "http://piyush-cosmo.github.io/reponame(remoteone)
         ----->scripts
              "predeploy"
              "deploy"
f_cosmo-->npm run deploy

$ git checkout gh-pages // go to the gh-pages branch
$ git rebase master // bring gh-pages up to date with master
$ git push origin gh-pages // commit the changes
$ git checkout master // return to the master branch
sync-ghpages = !git checkout gh-pages && git rebase master && git push origin gh-pages && git checkout master


